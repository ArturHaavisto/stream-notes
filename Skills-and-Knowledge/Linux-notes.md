- 3 major linux distribution families: Red Hat, SUSE, Debian
- Linux borrows from UNIX
![[Pasted image 20260208113112.png]]

# Boot loader
- Program that boots the operating system
- For example: GRUB and ISOLINUX

# Service
- Program that runs as a background process
- For example: httpd, nfsd, ntpd, ftpd, and named

# File system
- Method for storing and organizing files
- For example: ext3, ext4, FAT, XFS, NTFS, and Btrfs

# X Windows system
![[Pasted image 20260208113431.png]]

# Desktop environment
- Graphical user interface on top of the operating system
- For example: GNOME, KDE, Xfce, and Fluxbox

# Command line
- Interface for typing commands on top of the operating system

# Shell
- Command line interpreter that interprets the command line input and instructs the operating system to perform and necessary tasks and commands
- For example: bash, tcsh, and zsh

# Distros
## Red Hat Enterprise Linux (or RHEL)
- Fedora an upstream testing platform
- CentOS is a close clone of RHEL
- Intel x86, Arm, Itanium, PowerPC
- yum package manager
- Widely used by enterprises

## CentOS
## CentOS stream
## Fedora
## Oracle Linux
## SUSE
- SLES (SUSE linux enterprise server) is upstream for openSUSE
- RPM-based zypper package manager
- YaST for system admin purposes
- Widely used in retail
## Debian
- Debian family is upstream for Ubuntu
- DPKG-based APT package manager
- Widely used for cloud deployments
- GNOME-based but differs visually
## Ubuntu
## Linux Mint

## Scientific Linux
# Random
- systemd: a suite of basic building blocks for a Linux system. It provides a system and service manager that runs as PID 1 and starts the rest of the system.
- daemon: a background service (d as the last character in a name meant deamon)
- fork (system call): a process creates a copy of itself
- pacman: packet manager
- GNU: Richard Stallman
- MBR: Master boot record: The MBR holds the information on how the disc's sectors are divided into partitions
- GNU GRUB: Grand Unified Bootloader
- GNOME: GNU Object Model Environment